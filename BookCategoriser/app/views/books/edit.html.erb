<h1>Editing book</h1>
<%= render 'form' %>
<%= link_to 'Show', genre_book_path(@genre, @book) %> |
<%= link_to 'Back', genre_books_path(@genre) %>

<div class="container-fluid">
  <div class="row">
    <div class="col-lg-3">
    </div>
    <div class="col-lg-6 centredisplay">   
    <br />
    <p class="headingtext">Edit book in the <%= @genre.name %> section</p>
    <br />
        <%= render "form", book: @book %>
        <p id="notice"><%= notice %></p>
         <%= link_to "Back to genres", genre_books_path %>
    </div>
    <div class="col-lg-3">
    </div>
  </div>
</div>
